{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "yarn command:prod upgrade --verbose",
    "restartPolicyType": "NEVER",
    "restartPolicyMaxRetries": 1
  }
}
